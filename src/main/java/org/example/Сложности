1. Итерационный метод:
Пространственная сложность равна O(1), так как в данном методе используется постоянное количество переменных, независимо от входного значения n.
Временная сложность равна O(n), поскольку каждое число Фибоначчи вычисляется за константное время, и в цикле выполняется n итераций.

2. Рекурсивный метод:
Пространственная сложность равна O(n), где n - входное значение. Это связано с тем, что для каждого вызова функции выделяется память в стеке, и глубина стека будет пропорциональна n.
Временная сложность равна O(2^n). Это объясняется тем, что многие вычисления повторяются, и есть много избыточных вызовов функций.

3. Динамическое программирование:
Пространственная сложность равна O(n), так как используется массив фиксированного размера n+1 для хранения промежуточных результатов.
Временная сложность равна O(n). Это объясняется тем, что каждое число вычисляется за константное время, и всего выполняется n итераций в цикле.
